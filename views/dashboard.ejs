<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <%- include('includes/general_header') %>

        <main>
            <h3>Welcome to my dashboard, mina-san!</h3>
            <% if(posts.length===0) { %>
                <h4>You have no posts yet. Create one now! 🐧</a></h4>
                <% } else { %>
                    <h4>Hello, user <%= user.email %>. Here are your wibu posts:🐧 </h4>
                    <% } %>
                        <ul>
                            <% posts.forEach(post=> { %>
                                <li>
                                    <a href="/post/<%= post.id %>">
                                        <%= post.title %>
                                    </a> on <%= new Date(post.createdDate).getDate() %>/<%= new
                                            Date(post.createdDate).getMonth() + 1 %>/<%= new
                                                Date(post.createdDate).getFullYear() %>
                                </li>
                                <% }) %>

                        </ul>

        </main>

        <%- include('includes/general_footer') %>
</body>

</html>