<!DOCTYPE html>
<html lang="vi">


<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hồ sơ - <%= user.email %>
    </title>
    <link rel="stylesheet" href="styles.css">
</head>

<body>
    <%- include('includes/general_header') %>

        <main class="container">
            <h2>Chỉnh sửa hồ sơ</h2>

            <% if (errors.length) { %>
                <div class="error-message">
                    <% errors.forEach(error=> { %>
                        <p>
                            <%= error %>
                        </p>
                        <% }) %>
                </div>
                <% } %>

                    <form method="POST" action="/profile/update">
                        <div class="form-group">
                            <label>Họ và tên:</label>
                            <input type="text" name="fullname" value="<%= user.fullname %>" required>
                        </div>

                        <div class="form-group">
                            <label>Số điện thoại:</label>
                            <input type="tel" name="phone" value="<%= user.phone %>">
                        </div>

                        <div class="form-group">
                            <label>Địa chỉ:</label>
                            <input type="text" name="address" value="<%= user.address %>">
                        </div>

                        <div class="form-group">
                            <label>Giới thiệu bản thân:</label>
                            <textarea name="bio"><%= user.bio %></textarea>
                        </div>

                        <div class="form-group">
                            <label>Kỹ năng (JSON array):</label>
                            <textarea class="json-field" name="skills"><%= user.skills %></textarea>
                        </div>

                        <div class="form-group">
                            <label>Học vấn (JSON object):</label>
                            <textarea class="json-field" name="education"><%= user.education %></textarea>
                        </div>

                        <div class="form-group">
                            <label>Kinh nghiệm (JSON array):</label>
                            <textarea class="json-field" name="experience"><%= user.experience %></textarea>
                        </div>

                        <button type="submit">Cập nhật</button>
                        <a href="/profile" class="button">Hủy bỏ</a>
                    </form>
        </main>

        <%- include('includes/general_footer') %>
</body>

</html>