<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh sửa hồ sơ - <%= user.email %>
    </title>
    <link rel="stylesheet" href="https://cdn.simplecss.org/simple.min.css">
</head>

<body>
    <%- include('includes/general_header') %>

        <main>
            <h2>Chỉnh sửa hồ sơ</h2>

            <% if (errors.length) { %>
                <div class="error-message">
                    <% errors.forEach(error=> { %>
                        <p><strong>⚠️ </strong>
                            <%= error %>
                        </p>
                        <% }) %>
                </div>
                <% } %>

                    <form method="POST" action="/profile/update">
                        <article>
                            <label>
                                <span>Full Name</span>
                                <input type="text" name="fullname" value="<%= user.fullname || '' %>" required
                                    style="display: block; margin-top: 5px;">
                            </label>
                            <label>
                                <span>Phone Number</span>
                                <input type="tel" name="phone" value="<%= user.phone || '' %>" required
                                    style="display: block; margin-top: 5px;">
                            </label>
                            <label>
                                <span>Address</span>
                                <input type="text" name="address" value="<%= user.address || '' %>" required
                                    style="display: block; margin-top: 5px;">
                            </label>
                            <label>
                                <span>Bio</span>
                                <textarea name="bio" required
                                    style="display: block; margin-top: 5px;"><%= user.bio || '' %></textarea>
                            </label>
                        </article>

                        <article>
                            <label for="skills">Skills</label>
                            <textarea class="json-field" id="skills" name="skills"><%= user.skills || '[]' %></textarea>

                            <label for="education">Education</label>
                            <textarea class="json-field" id="education"
                                name="education"><%= user.education || '{}' %></textarea>

                            <label for="experience">Experience</label>
                            <textarea class="json-field" id="experience"
                                name="experience"><%= user.experience || '[]' %></textarea>
                        </article>

                        <div class="button-group">
                            <button type="submit" class="primary">Cập nhật</button>
                            <a href="/profile" class="button secondary">Hủy bỏ</a>
                        </div>
                    </form>
        </main>

        <%- include('includes/general_footer') %>
</body>

</html>